#include <stdio.h>
#include <string.h>
int main()
{
    char name[100], copy[100], d;
    printf("Enter the Name:");
    gets(name);
    for(int i=0;name[i]!=\0;i++){


    }
    return 0;
}